# Core framework (code targets Django 2.2 LTS: uses on_delete=, django.conf.urls.url, etc.)
Django==2.2.28
djangorestframework==3.11.0
djangorestframework-simplejwt==4.6.0
PyJWT==1.7.1
django-cors-headers==3.4.0
django-extensions==2.2.9

# Database driver (binary wheel -> no system build deps needed)
psycopg2-binary==2.8.6

# Imported by api/views.py
requests==2.25.1

# Production server + deployment helpers
gunicorn==20.1.0
whitenoise==5.3.0
dj-database-url==0.5.0
